# Copy to .env.local for local dev, or set as a build env var in production.
# Local dev default:
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
# Production example (no trailing slash; must be set BEFORE `npm run build`):
# NEXT_PUBLIC_API_URL=https://your-backend-host.onrender.com

